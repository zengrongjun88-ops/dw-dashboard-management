<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dw.dashboard.mapper.UserMapper">

    <!-- 通用结果映射 -->
    <resultMap id="BaseResultMap" type="com.dw.dashboard.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="status" property="status"/>
        <result column="deleted" property="deleted"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, username, password, real_name, email, phone, status,
        deleted, create_time, update_time, create_by, update_by
    </sql>

    <!-- 根据用户名查询用户及其角色 -->
    <select id="selectUserWithRoles" resultType="com.dw.dashboard.entity.User">
        SELECT
            u.id,
            u.username,
            u.password,
            u.real_name,
            u.email,
            u.phone,
            u.status
        FROM tb_user u
        WHERE u.username = #{username}
          AND u.deleted = 0
    </select>

</mapper>
